name = offhand_screen_view
title = Offhand Screen View (client companion)
description = Reports the camera mode to the offhand_screen_view server mod so the offhand icon is only shown in first person.
